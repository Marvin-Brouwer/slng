{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Launch Extension",
			"type": "extensionHost",
			"request": "launch",
			"showAsyncStacks": true,
			// TODO we disabled this to remove the 4 console windows on startup
			// Figure out if we even need this flag
			// "autoAttachChildProcesses": true,
			"windows": {
				"internalConsoleOptions": "neverOpen"
			},
			"args": [
				// TODO figure out a way to have vscode actually work with the correct log level
				"--log-level trace",
				"--log slng.@sling/vscode:debug",
				"--disable-updates",
				"--disable-workspace-trust",
				"--profile-temp",
				"--skip-release-notes",
				"--skip-welcome",
				"${workspaceFolder}/examples",
				"--disable-extension vscode.git", // Disable built-in git to disable parent git message
				"--enable-extension ms-vscode-remote.remote-wsl", // Enable remoting
				"--extensionDevelopmentPath=${workspaceFolder}/packages/vscode-extension"
			],
			"skipFiles": [
				"<node_internals>/**",
				"**/node_modules/**"
			],
			"preLaunchTask": "build:vscode"
		}
	]
}